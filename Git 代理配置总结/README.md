2026.6.25总结，因为在大三下的《大数据计算及应用》课程上传代码仓库时，发现怎么都git clone不了我们的代码仓库到本地。
- 初始时以为是网络或者梯子问题,但是在等了一个晚上后第二天再试仍然不行

因此经调试发现是我一直都没有搞懂git代理配置,以及VPN的配置中"系统代理"的局限性,即：

- Clash 的系统代理模式仅影响“主动读取系统代理”的应用（如浏览器），Git 不属于此类。
- 需要显式为 Git 配置代理，使其流量经过 Clash 的本地代理端口。

因此在当时进行了问题发现、解决与总结
