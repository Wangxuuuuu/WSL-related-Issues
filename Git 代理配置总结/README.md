### 2026.6.25总结:"Git 代理配置总结报告.md"
因为在大三下的《大数据计算及应用》课程上传代码仓库时，发现怎么都git clone不了我们的代码仓库到本地。
- 初始时以为是网络或者梯子问题,但是在等了一个晚上后第二天再试仍然不行

因此经调试发现是我一直都没有搞懂git代理配置,以及VPN的配置中"系统代理"的局限性,即：

- Clash 的系统代理模式仅影响“主动读取系统代理”的应用（如浏览器），Git 不属于此类。
- 需要显式为 Git 配置代理，使其流量经过 Clash 的本地代理端口。

因此在当时进行了问题发现、解决与总结

---

### 2026.7.3补充总结:"WSL Git 代理配置及路径兼容性问题解决报告.md":

因为在《数据安全》课程上传"实验五"时，在Windows中 git clone 遇到"实验二"遗留的路径兼容性问题（文件名含冒号），然后转而尝试WSL的git clone.
然后发现WSL中也无法连接GitHub.

经调试发现根本原因是WSL不继承Windows系统代理，且Windows防火墙拦截了WSL的入站连接。经调试解决了:

- WSL中Git代理配置
- Windows防火墙放行
- 以及路径修复等问题

并形成本报告。

因此在当时进行了问题发现、解决与总结